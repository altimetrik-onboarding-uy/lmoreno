({
    init: function (cmp, event, helper) {
        helper.initialList(cmp, event, helper);      
    },
    
	change: function (cmp, event, helper) {
        helper.helperChange(cmp, event, helper);	
	}    
})